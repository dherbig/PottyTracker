#include <Arduino.h>

// Phase 3: WiFi, HTTP client, buttons, motor driver.
// See README.md for API contract and PlatformIO commands.

void setup() {
  Serial.begin(115200);
  Serial.println("Potty Tracker ESP32 clock — firmware scaffold");
}

void loop() {
  delay(1000);
}
